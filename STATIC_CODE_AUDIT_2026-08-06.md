# 静态代码审核报告（2026-08-06）

## 1. 审核范围

本轮审核基于开发分支 `agent/multi-product-line-cdp` 的提交 `5409b1e`，覆盖：

- 8787 Amazon 竞品监控入口；
- 8790 领星经营报告入口；
- 领星接口同步、14 天滚动更新与历史冻结；
- 目标表导入与目标完成率；
- DeepSeek 分析编排；
- Word 生成、图表和报告归档；
- SQLite 数据访问；
- Windows 本地代理与文件名边界。

本轮未修改 PRD、主 TODO 或 `todo_progress.md`。

## 2. 已确认并修复的问题

### 高优先级

1. **竞品抓取存在重复任务竞态**
   - 两次相近的 `/api/crawl` 请求可能在后台协程将 `running` 置为真之前同时通过检查。
   - 修复：在创建后台任务前同步预占运行状态；任务创建失败时释放预占。

2. **领星同步存在重复任务竞态**
   - `/api/reporting/sync` 原先先读取状态、再创建任务，存在相同的检查与执行间竞态。
   - 修复：增加线程安全的同步启动预占；重复请求稳定返回 409。

3. **领星配置加载失败可能使页面永久显示“正在同步”**
   - `AuditSettings.load()` 与同步记录创建发生在异常清理范围之外。
   - 修复：把配置加载、同步记录创建和 API 初始化全部纳入统一失败收口，确保状态回落。

4. **SQLite 连接没有真正关闭**
   - `sqlite3.Connection` 的上下文管理器只提交或回滚，不负责关闭连接；原代码大量使用 `with connection()`，在 Python 3.13 下产生资源泄漏警告，在 Windows 上可能造成数据库文件长期占用。
   - 修复：两套数据库连接统一改为显式关闭的上下文管理器，并以 `ResourceWarning` 作为测试失败条件验证。

5. **广告比率采用算术平均，产品线指标失真**
   - CTR、CPC、CVR、ACOS、ROAS 原先平均各行比率，未按曝光、点击、花费和销售额加权。
   - 修复：优先由可加总基础指标重新计算；只有基础指标缺失时才回退到原始比率平均。

6. **多 Listing 库存只取到一条记录**
   - 产品线的 FBA 可售、在途和预留库存原先从整个周期选一条“最新记录”，会漏掉其他 Listing。
   - 修复：每个 Listing 分别取最新值，再按产品线求和。

7. **实际销售额为 0 时会被滚动 Listing 数据覆盖**
   - 使用真假值判断把合法的 `0` 当成缺失值。
   - 修复：只在值为 `None` 时启用滚动口径回退。

8. **月度看板没有加载上一个等长周期**
   - 月末只读取本月起始日以后的数据，月度环比无法计算。
   - 修复：根据当月已过天数向前加载同等长度的历史区间。

### 中优先级

9. **本机 9222 检查可能被系统代理或 VPN 接管**
   - `urllib.request.urlopen` 默认读取代理环境变量。
   - 修复：CDP 本机请求使用显式无代理 opener。

10. **SellerSprite 解析可能误用其他 ASIN 的面板**
    - 找不到目标 ASIN 面板时，原代码会退化为页面上的任意快速查看面板。
    - 修复：目标面板不存在即返回不可用，不保存其他产品的数据。

11. **目标表读取后未显式关闭工作簿**
    - Windows 下可能导致 `目标表.xlsx` 暂时无法覆盖或删除。
    - 修复：模板生成和目标导入均显式关闭工作簿。

12. **目标值允许 NaN 或无穷大进入数据库**
    - `float("nan")` 和 `float("inf")` 能通过原数值解析。
    - 修复：所有目标值必须是有限数值。

13. **报告归档下载不校验文件内容**
    - 数据库保存了 SHA-256，但下载时未使用；归档文件被替换后仍会下载。
    - 修复：下载前重新计算 SHA-256，不一致时拒绝返回。

14. **超长产品线名称可能触发 Windows 路径限制**
    - 产品线最长允许 200 字符，叠加日期和报告类型后可能超过常见 Windows 路径边界。
    - 修复：文件名组件限制为 80 字符，截断后附加稳定哈希避免重名。

15. **报告归档失败后遗留未登记 Word 文件**
    - Word 保存成功但数据库归档失败时，目录中会出现无法从历史记录下载的孤立文件。
    - 修复：归档失败时删除本次新生成文件并继续抛出异常。

16. **目标计算依赖运行时 monkeypatch**
    - `app.reporting.__init__` 在导入阶段替换 `targets.targets_for_period`，静态分析和直接模块调用难以确定真实实现。
    - 修复：看板直接依赖单品感知的目标计算模块；旧导入路径保留显式包装函数，不再运行时替换。

17. **领星根地址可携带账号、密码或查询参数**
    - 配置摘要可能间接暴露嵌入 URL 的敏感信息。
    - 修复：拒绝包含 userinfo、query 或 fragment 的 `LINGXING_BASE_URL`。

18. **自定义领星根地址被静默忽略**
    - 当前 SDK 固定使用官方 OpenAPI 地址。
    - 修复：当配置不是官方地址时，把限制写入同步警告，不再静默误导。

## 3. 新增回归测试

新增 `tests/test_static_audit_regressions.py`，覆盖：

- 两套后台任务的启动预占、重复请求和创建失败回滚；
- CDP 本机请求绕过代理；
- 配置加载失败后的同步状态恢复；
- 同步状态返回值的可变对象隔离；
- 广告比率加权计算；
- 多 Listing 最新库存汇总；
- 目标值有限性；
- 目标表工作簿关闭；
- 报告归档 SHA-256 校验；
- Windows 安全文件名；
- SQLite 连接关闭；
- 零销售额不触发回退；
- 月度等长周期比较；
- SellerSprite 不跨 ASIN 取值；
- 领星根地址敏感信息校验。

## 4. 本地验证结果

```text
python -m compileall -q app tests        通过
git diff --check                        通过
pytest -q                               54 passed
pytest -q -W error::ResourceWarning     54 passed
```

## 5. 尚未由静态测试覆盖的风险

1. 真实领星账号的订单、售后、库存和广告接口仍需本机验收；模拟响应无法覆盖服务端字段临时变化、权限差异与限流行为。
2. 领星 SDK 通过函数签名动态适配分页和日期参数；SDK 升级后仍需执行接口盘点与真实同步回归。
3. SQLite 写锁只覆盖当前 Python 进程；不支持多个 8790 服务实例同时写同一数据库。
4. DeepSeek 的网络调用、模型可用性与输出限制只做了模拟测试，真实 Key 仍需生成一次完整报告验收。
5. Amazon 和 SellerSprite 页面结构属于外部依赖，页面改版后解析选择器可能失效，但失败会以部分成功或失败状态记录，不会伪造数据。
