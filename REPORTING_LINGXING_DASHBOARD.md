# 领星经营报告看板

## 当前范围

报告程序与竞品监控程序相互独立。本阶段只实现领星经营数据、重点产品线看板、人工记录和 Word 报告；不接入竞品抓取，也不在网页中维护目标。

## 数据来源

日常运行直接读取领星：

```text
领星店铺
→ 领星 Listing 目录
→ 最近14天销售、售后、库存和SP广告数据
→ data/reporting.db
→ 经营看板与Word报告
```

第一阶段的接口盘点文件只用于诊断，不再作为日常报告数据源。报告程序不读取 `产品输入表.xlsx` 或 `data/monitor.db`。

## 产品设置

设置页按国家和店铺展示领星 Listing，并支持产品搜索。

每个 Listing 只有两个互斥按钮：

- `重点`：我负责的重点产品；
- `普通`：我负责的普通产品；
- 两个都不选：不归我负责。

不归我负责的 Listing 仅保留在产品目录中，以便以后重新分配；程序不会把其经营指标写入报告数据库，也不会在主看板或 Word 报告中展示。

负责产品需要填写产品线，例如“足球门”。同一产品线下可以包含多个店铺、国家和 Listing。

## 数据同步规则

“同步最新数据”执行两类工作：

1. 刷新领星店铺与完整 Listing 目录；
2. 只为重点和普通产品同步最近14天经营数据。

滚动窗口包含当天在内的14个自然日。窗口内的数据允许重复更新，用于吸收广告归因、退款和订单状态变化。早于窗口起点的数据被标记为最终历史，后续同步不能覆盖。

## 主看板

主页面只展示重点产品线。每条重点产品线包含：

- 当日；
- 最近3日；
- 最近7日；
- 最近14日；
- 本月。

每个周期左侧展示指标、趋势图和数据缺失提示；右侧填写操作、原因、判断、计划和需要的支持。记录可手动保存，每60秒自动保存，并保留旧版本。

## Word报告

每条重点产品线支持下载：

- 日报；
- 周报；
- 月报。

数字、环比、TACOS和表格由程序计算。若 `.env` 配置了 DeepSeek，程序会把确定数据与人工记录发送给模型生成文字分析；模型被要求不得创造或修改数字。未配置或调用失败时，使用规则版分析生成 Word。

## 当前已知数据限制

- 领星 ASIN 利润接口当前权限失效，因此不会生成虚构利润；
- FBA库龄接口当前权限失效；
- Listing SDK响应模型与真实返回不一致，程序绕过响应模型校验，保留领星原始 Listing 数据；
- 目标将在后续通过 Excel 导入，本页面不提供目标输入。
