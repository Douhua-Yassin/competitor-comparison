# 第四阶段静态代码审核报告

- 审核日期：2026-08-06
- 审核基线：`1a3fe0a80477e655d5605f47b4f2019235573498`
- 审核分支：`review/static-audit-phase4-20260806`
- 审核范围：领星经营报告、最近14天同步验收、设置页范围管理、验收导出及其与8787基础服务的依赖边界

## 1. 结论

本轮确认并修复11项可复现问题，其中高优先级5项、中优先级6项。修复集中在同步运行状态、负责范围一致性、敏感信息脱敏、验收文件完整性和前端刷新机制。

当前自动测试、Windows服务烟测和Chrome CDP回归均已通过。真实领星账号、真实DeepSeek Key及当前用户SellerSprite插件页面仍需本机验收。

## 2. 高优先级问题

### 2.1 同步中的半成品被当作完成结果

原验收构建器只读取数据库最近一次运行。运行状态仍为`running`时，它仍会读取已经写入的部分指标并返回`ready`，导致页面显示不完整覆盖率。

修复：

- 同时读取数据库运行记录与当前进程内同步状态；
- 真实运行中返回`syncing`；
- 清空接口覆盖和产品线覆盖，不使用半成品数据；
- 同步完成后再生成正式验收结果。

### 2.2 服务异常退出后永久显示同步中

进程被强制关闭时，数据库中的运行记录可能保留`running`。重启后进程内没有任务，但旧页面仍会被判断为同步中。

修复：

- 数据库为`running`且当前进程没有同步任务时返回`interrupted`；
- 明确提示上一次同步未正常结束，需要重新同步；
- 不展示旧运行的半成品覆盖结果。

### 2.3 同步期间允许修改负责范围

同步开始后仍可修改重点、普通和产品线，可能使实际请求范围、写入指标和最终验收范围不一致。

修复：

- `PUT /api/reporting/products/scope`在同步运行期间返回HTTP 409；
- 同步结束后恢复正常修改。

### 2.4 敏感信息脱敏覆盖不足

原规则主要覆盖AppSecret、Access Token、签名和Bearer Token，未完整覆盖AppID、client secret、refresh token、id token、API key、普通Authorization及URL查询参数。

修复：

- 递归清洗API、Markdown和JSON中的全部字符串；
- 增加AppID、AppSecret、client secret、API key、access/refresh/id token、generic token、Authorization、Bearer、signature和sign；
- 清洗URL查询参数中的凭证；
- 参数化测试逐项验证，不依赖一个综合文本间接覆盖。

### 2.5 Authorization可能残留令牌正文

当字符串包含`Authorization: Bearer <token>`时，错误的替换顺序可能只处理部分前缀。

修复：

- 优先按完整Authorization头替换；
- 再执行Bearer和普通键值脱敏；
- Basic与Bearer均有独立测试。

## 3. 中优先级问题

### 3.1 等量替换或修改产品线时漏报范围变化

只比较“同步时负责产品数量”和“当前负责产品数量”，无法识别数量相同但具体Listing或产品线已经变化的情况。

修复：

- 比较同步完成时间与当前Listing的`updated_at`；
- 列出同步后变化的产品线和MSKU；
- 提示重新同步。

边界：历史数据库中同一秒内完成同步和修改范围的记录可能无法追溯。

### 3.2 前端验收面板可能保持旧内容

原前端只在同步运行编号改变时重新渲染。修改负责范围不会改变运行编号，因此验收提示可能不更新。

修复：

- 每5秒读取最新验收结果并重新渲染；
- 运行中显示专用状态；
- 不依赖运行编号判断是否刷新。

### 3.3 导出失败可能留下半文件

原验收导出直接写最终文件。磁盘异常时可能留下不完整文件或空运行目录。

修复：

- 先写同目录临时文件；
- 成功后原子替换最终文件；
- 失败时清理临时文件和本次运行目录。

### 3.4 Markdown动态文本可能破坏结构

产品线、告警或其他动态文本包含管道符、反引号或反斜杠时，可能破坏表格或代码标记。

修复：统一转义反斜杠、管道符和反引号。

### 3.5 限流、网络和超时未列为阻塞问题

429、rate limit、network和timeout虽然会使数据缺失，原验收只重点识别认证与403。

修复：将401、403、429、授权、认证、白名单、限流、网络和超时统一纳入需要处理区域。

### 3.6 时间戳时区格式不一致

SQLite历史记录可能同时包含有时区和无时区ISO时间，直接比较会抛出异常。

修复：无时区时间统一补为UTC后比较。

## 4. 新增测试

新增：

- `tests/test_acceptance_service_hardening.py`
- `tests/test_acceptance_secret_redaction_matrix.py`

覆盖：

- 运行中同步不使用半成品指标；
- 残留`running`识别为中断；
- 同步期间范围修改返回409；
- 产品数量不变但产品线变化仍能识别；
- 401/403/429、限流、网络和超时告警；
- 完整凭证脱敏矩阵；
- 原子导出及失败清理；
- Markdown转义；
- 有时区和无时区时间兼容；
- 非敏感诊断文本不被错误修改。

## 5. 自动验证结果

### Windows Tests

Run：`31076988218`

- Python 3.10：成功；
- Python 3.11：成功；
- 完整Pytest：`79 passed, 4 skipped`；
- Python编译：成功；
- Ruff致命错误检查：成功；
- JavaScript语法检查：成功；
- PowerShell 5.1解析：成功；
- 8787真实启动和停止：成功；
- 8790真实启动、API烟测和停止：成功；
- 旧式系统Python服务停止：成功；
- Chrome 9222与Playwright CDP：成功，`contexts=1 pages=1`。

### Reporting Next Stage

Run：`31076988214`

- 报告专项测试：`9 passed`；
- 目标和图表依赖：成功；
- Word视觉样例生成：成功；
- 报告前端脚本语法：成功。

说明：CI出现Starlette对旧TestClient/httpx组合的弃用警告，以及GitHub Actions运行时的Node版本迁移警告。两者均未影响本次功能测试，后续依赖升级时应单独处理。

## 6. 未由自动测试证明的事项

- 真实领星账号最近14天的分页、权限、限流和字段完整性；
- 真实DeepSeek Key的结构化输出和失败回退；
- 当前用户Chrome中的SellerSprite插件页面结构；
- 多个Uvicorn进程同时写同一个`reporting.db`；
- 进程被操作系统强制终止时的全部外部资源状态。

## 7. 合并条件

- 最终head的Python 3.10和3.11 Windows测试通过；
- Reporting Next Stage专项通过；
- PR只合并至`agent/multi-product-line-cdp`；
- `main`保持不变。
